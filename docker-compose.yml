version: '3.8'

services:
  api:
    build: ./api
    ports:
      - 8080:8080
    env_file:
      - ./api/.env
  # dashboard:
  #   build: ./dashboard
  #   ports:
  #     - 3000:3000
  #   env_file:
  #     - ./dashboard/.env
  # db:
  #   image: mysql:8.0
  #   cap_add:
  #     - SYS_NICE
  #   restart: always
  #   environment:
  #     - MYSQL_DATABASE=security-scans
  #     - MYSQL_ROOT_PASSWORD=mauFJcuf5dhRMQrjj
  #   ports:
  #     - '3306:3306'
  #   volumes:
  #     - db:/var/lib/mysql
  #     - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  db:
    driver: local 
