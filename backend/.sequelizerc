const path = require('path');
require('dotenv').config();

module.exports = {
  config: path.resolve('', 'sequelize.config.js'),
  'models-path': path.resolve('src', 'models'),
  'migrations-path': path.resolve('src/db-generator', 'migrations'),
  'seeders-path': path.resolve('src/db-generator', 'seeders')
};
